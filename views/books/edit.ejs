<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book</title>
</head>
<body>
    <form action="/books/<%=book._id%>?_method=PUT" method="POST">
        <div>
            <label for="title">Title</label>
            <input type="text" name="book[title]" id="title" value="<%=book.title%>" >
        </div>
        <div>
            <label for="author">Author</label>
            <input type="text" name="book[author]" id="author" value="<%=book.author%>">
        </div>
        <div>
            <label for="isbn">ISBN</label>
            <input type="text" name="book[isbn]" id="isbn" value="<%=book.isbn%>">
        </div>
        <div>
            <label for="published_date">Published Date</label>
            <input type="date" name="book[published_date]" id="published_date" value="<%=book.published_date%>">
        </div>
        <div>
            <label for="copies">Copies</label>
            <input type="number" name="book[copies]" id="copies" value="<%=book.copies%>">
        </div>
        <div>
            <label for="copies_available">Available Copies</label>
            <input type="number" name="book[copies_available]" id="copies_available" value="<%=book.copies_available%>">
        </div>
        <button type="submit">Update Book</button>
    </form>
</body>
</html>